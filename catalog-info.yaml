# Backstage catalog entry, read by paved's scorecard plugin.
#
# Every check paved scores is derived from this file alone — no cluster
# access, no CI credentials. What is absent here is absent from the score.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: terraform-helm-scaphandre
  description: >-
      Terraform module to deploy Scaphandre (container-level power consumption monitoring) on Kubernetes
      using Helm.
  annotations:
    github.com/project-slug: fabiocicerchia/terraform-helm-scaphandre
    backstage.io/source-location: url:https://github.com/fabiocicerchia/terraform-helm-scaphandre/
  links:
    - url: https://github.com/fabiocicerchia/terraform-helm-scaphandre
      title: Source
spec:
  type: library
  lifecycle: production
  owner: group:default/kodebeat
  system: oss-tools
